<UserControl x:Class="Hma.Desktop.Wpf.Presentation.Features.Routes.Views.RouteCatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:Hma.Desktop.Wpf.Presentation.Features.Routes.Views">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource BorderDefaultBrush}"
                BorderThickness="0,0,1,0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Tuyến đường" Style="{StaticResource WorkspaceTitle}" Margin="16,24,16,16"/>
                <ListBox ItemsSource="{Binding Sections}" SelectedItem="{Binding SelectedSection}"
                         Background="Transparent" BorderThickness="0" Margin="8,0,8,16"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ItemContainerStyle="{StaticResource HubNavListBoxItem}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Title}" FontSize="15"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>
        <Grid Grid.Column="1">
            <Grid Visibility="{Binding IsLocationsSelected, Converter={StaticResource BoolToVisibility}}">
                <views:LocationView DataContext="{Binding Locations}"/>
            </Grid>
            <Grid Visibility="{Binding IsRoutesSelected, Converter={StaticResource BoolToVisibility}}">
                <views:RouteView DataContext="{Binding Routes}"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
